# 运行数据（不进版本库）
# 2026-09-16 修正：原为 `web-relay/experiments/`——**含中间斜杠的模式锚定在仓库根**，
# 因此当 relay 因未传 workspacePath 而以宿主 cwd（bin/）为基准落盘、生成 `bin/web-relay/` 时
# 不被忽略 → `git status` 报 `?? bin/web-relay/` → 验收器把该目录当文件读 → EISDIR 崩溃 →
# 链条判 REJECT → 因 verify-rejected 可重派而形成**每 20 分钟一次的重派循环**（实测 25 次）。
# 改为 `**/` 前缀使其在**任意层级**生效，避免同类污染再次击穿验收器。
**/web-relay/experiments/
**/web-relay/traces/

# 备份与临时产物
*.bak*
*.decoded
*.log

# Node
node_modules/

# 会话分析临时脚本（如确需入库请显式添加）

# watchdog 运行时单例锁
bin/.watchdog.lock

# 重启请求信号文件（A8，2026-09-16）：由 `watchdog.mjs request-restart` 写入、被常驻 watchdog
# 消费后删除；**在「重启待执行」的窗口内文件存在**，若不忽略会让仓库呈现「脏」——
# 此时任何 `verify-cc-task` 都会把它当成越界改动 → REJECT（回归套件也要求工作树必须干净）。
bin/restart.request.json
