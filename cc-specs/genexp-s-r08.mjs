// S 组复本 r08（由 gen-genexp-specs.mjs 生成，请勿手改）：**prompt 与 S 组逐字节相同**，仅 taskId 不同。
export const task = {
  taskId: 'ccgen-20260916-prop-s-r08',
  kind: 'understand',
  title: '生成类实验 S 组复本 r08（机器可读结论块）',
  refs: ['lib/index.js', 'bin/watchdog.mjs'],
  anchors: [
    { file: 'lib/cc-stats.mjs', pattern: 'export function summarizeChainTasks', note: '仓库存在的符号之一' },
  ],
  acceptance: '同 S 组：out/report.md 含严格格式 GEN-RESULT 块（情境 1..4 各 ALTERNATIVE/RISK 一行）；必须写 done.flag；不得修改仓库任何文件（写入范围为空）。',
  expectArtifacts: ['report.md'],
  outputDir: 'out',
  writeScope: [],
  prompt: `你是 dsh-web-relay 项目组的资深架构师。下面给出 4 个**设计情境**（均为真实工程场景，但只给出决策前的上下文）。
请对每个情境**独立**给出你的设计建议。

仓库根：/mnt/d/dsh-web-relay（只读；你可以读它来了解现状，但**不得修改任何文件**）

【约束】
- **不得修改仓库任何文件**；只允许读取与检索。
- 产物写你自己的工作目录 out/report.md。
- **完成后必须写入完成标记 done.flag**（路径见任务契约）——只写产物不写标记会被判为失败。
- 不要猜测本任务的目的或情境的来源。

【必须输出机器可读块】报告 out/report.md 中必须包含如下块（严格格式，逐案例、顺序与编号一致）：

GEN-RESULT-BEGIN
CASE 1:
ALTERNATIVE: <一句话：你最终推荐的做法>
RISK: <一句话：在你所推荐的做法下，最可能失效的地方>
CASE 2:
ALTERNATIVE: ...
RISK: ...
CASE 3:
ALTERNATIVE: ...
RISK: ...
CASE 4:
ALTERNATIVE: ...
RISK: ...
GEN-RESULT-END

块之后，每个案例另附 3-6 行说明：你为什么这样设计、考虑过哪些替代、为什么排除它们。

【工作方式（S 组）】
按你作为资深架构师的常规判断，对每个情境给出你认为最合适的做法，并说明关键取舍。

================ 情境（共 4 个） ================

【情境 1】链条状态文件的设计
某自动化链条包含若干待办项，由 Windows 计划任务每 20 分钟触发一次执行；
执行体把进度持久化到一个 JSON 状态文件里，以便断点续跑、并在全部跑完后不再重复派发。
现在要设计这个状态文件与"已跑完"判定。请给出你的设计。

【情境 2】诊断模式的状态副作用
某脚本有正常模式与两个诊断模式（--probe-only / --selftest-quota）。三种模式共用同一段初始化代码，
初始化会载入状态文件、并在某些情况下改写状态（例如清空某个字段）。诊断模式要求"无副作用"。
现在要设计保证方式。请给出你的做法。

【情境 3】把代码交付到运行时副本
插件在仓库中开发，但运行时加载的是另一份安装目录下的副本（由配置文件按包名挂载）。
现有一个交付脚本：它遍历包内文件清单，**只补齐目标端缺失的文件**；对已存在但内容不同的文件只报告、不覆盖。
现在要保证"改动能真的生效"。请给出你的设计。

【情境 4】任务粒度安排
要完成一个改动：涉及 5 个文件（含新增模块、接线到主入口、补测试、更新文档），并要求给出性能实测数字。
执行体是外部编码代理，单次执行有硬性时长上限，且与其额度配额共享。
现在要安排任务粒度。请给出你的做法。

================ 情境结束 ================`,
};
