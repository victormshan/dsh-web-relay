// 记录：设计2 结果（散文反证 vs 可执行反例，判定者=执行探针）+ 三次自身失误 + 三轮合并结论
import fs from 'node:fs';

const trace = String.raw`D:\dsh relay test\web-relay\traces\expr-2026-09-13_17-36-07.md`;
const now = new Date().toISOString();

const body = `
## [主 agent] ${now} — 设计2 结果：散文反证 vs 可执行反例（判定者=执行探针，无模型裁判）

【执行】链条 v8-exec（16 项交错，04:23–08:33Z 由配额窗口推进）16/16 完成。

【预登记判定（v1 探针）】P 8/24=33.3% ｜ E 12/24=50.0% ｜ 差 +16.7pp ｜ p=0.3806 → **无可检出差异**。
  但逐案例明细异常：案例2 全员通过、案例3 全员失败，且**每轮失败分数完全相同**（1 恒 6/7、3 恒 4/6）
  → 系统性信号，非模型随机。诊断出**v1 探针在案例3 上不公平**：带 bug 源码注释只定义 prev={status, at}，
  未定义任何重试计数字段，而 v1 只喂我参考实现私有的 failCount，等于逼候选猜我的字段名。

【公平性修正（v2 探针，事后修正，不冒充预登记结论）】同时喂 attempts/failCount/failures/retryCount 别名：
  P: 16/24=**66.7%**（案例1 0/8、案例2 8/8、案例3 8/8）
  E: 20/24=**83.3%**（案例1 4/8、案例2 8/8、案例3 8/8）
  差 +16.7pp，p=**0.3142** → 无可检出差异。

【诚实解读】有效比较**只有案例1**（n=8/臂）——案例2/3 在公平探针下两臂皆 8/8，零判别力。
  案例1 单看 P 0/8 vs E 4/8（Fisher 双尾 p≈0.077、单尾≈0.038）：**方向支持"可执行反例"**但不显著；
  机制可解释：E 的纪律迫使候选**实际运行验证**，从而发现"仅做文件类型过滤仍把未跟踪目录当文件返回"的枚举缺口。
  E 组 **4 次 NO_REPRO**（构造不出反例）→ 机制遵守度部分成立。
  E 报告反而更短（80659 vs 99879 字节）→ 可执行纪律**替换**了散文而非叠加（与第一轮"过程多→文本长"的担忧相反）。

【三轮合并结论（写入报告 §11.6）】
  一：评审协议能否发现既存缺陷 → 两臂相同（各 3/5、误报 0/2）→ 无收益（基线已达天花板）
  二：多角色反证能否想出更优替代 → 成对盲评 36%（CI [20.2,55.5]）→ 无显著差异；第一轮的"显著优势"= 仪器伪影
  三：可执行反例 vs 散文批判 → 66.7% vs 83.3%，p=0.31 → 无可检出差异
  **三轮一致**：增加"协商/角色/轮次"这类自然语言结构，未观察到可检出的质量提升；
  唯一正信号来自**证据生产**（要求跑反例 → 案例1 从 0/8 抬到 4/8），与 §8 结论同向。

【对"异构多模型分饰角色"的直接含义】该方案押注的机制（角色/性格互补）在三轮实验中均无效应证据；
  而它自己预留的"工程判决"（转向 Execution-Guided）恰是唯一有正信号的方向。
  → **在投入数天做异构委员会之前，最便宜的验证已指出收益更可能在执行接地，而不在模型多样性。**
  尚未回答的：真正的**异构参与者**（不同权重 + 不同工具权限）**没有被测过**——三轮都只变协议。
  若要做，应按**访问权限**做单变量实验（工具开/关），而不是按"模型性格"分工；且裁判必须族外
  （本机现实：Anthropic 无密钥、cc 是提案人、Gemini 无工具 → 干净的族外裁判几乎不存在，这本身就是对该设计的硬约束）。

【本轮我自己的三次失误（均当场修、均留痕）】
  ① 参考修复 line.trim().slice(3) 切进文件名 → 探针自检当场抓出（否则会用误杀正确修复的判定器跑 16 个任务）；
  ② 预登记脚本正文里用反引号 → 模板字符串截断 SyntaxError（今天第三次）→ 已在文件头写死硬规则；
  ③ 复核脚本用 b+20 切片（实际标记长 22）→ 16 份源码被切坏 → 误得"案例3 在 v2 下仍 0/16"的**假结论**；
     用正确切片重跑后为 8/8。共性：**三次都是测量/解析环节出错，不是被测对象**——与前三轮"验证器自己出问题"同型。

【交付物】_EXPERIMENTS-2026-09-16.md 新增 §11；证据 D:\\cc-tasks\\exec-result.json、exec-labels.json、
  probes/run-probe.mjs（v2）、probes/selftest-probes.mjs、analyze-exec.mjs、verify-exec-specs.mjs（14/14）。
`;

fs.appendFileSync(trace, body, 'utf8');
console.log('  追加字节 =', Buffer.byteLength(body, 'utf8'), '| 轨迹现字节 =', fs.statSync(trace).size);
