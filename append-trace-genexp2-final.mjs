// 记录：第二轮（out-of-sample）生成类实验的最终结论
import fs from 'node:fs';

const trace = String.raw`D:\dsh relay test\web-relay\traces\expr-2026-09-13_17-36-07.md`;
const now = new Date().toISOString();

const body = `
## [主 agent] ${now} — 第二轮生成类实验结论：仪器自检通过，**未观察到协议带来的提升**

【执行】链条 v7-gen2（新指针式装载器装填，未再手改 run-chain.cmd）16/16 全部 done、
块完整、16 个唯一时间戳（无复用）；17:06–19:53 由配额窗口推进。D 报告平均比 S 长 8%（正是必须风格归一的原因）。

【仪器自检（采信结论的前提）】**锚点 5/5 全部通过**——裁判在 5 个案例中都正确选中"金标准答案"而非"敷衍答案"，
说明改进后的仪器在本轮材料上**已证明有区分力**。这解决了第一轮"仪器不可信"的核心问题。

【主指标：成对盲评（风格归一后，220 字符统一截断）】
  案例1 洞察 D3/S3/平2 ｜ 案例2 洞察 D2/S1/平5 ｜ 案例3 洞察 **D0/S7/平1**
  案例4 对照 D2/S3/平3 ｜ 案例5 对照 D2/S2/平4
  合计 **D 9 : S 16（平 15）**，D 胜率 **36.0%**，Wilson 95% CI **[20.2%, 55.5%]**
  → CI 跨 50% → **本规模下两臂无显著差异**（点估计偏 S）。按预登记规则：**不主张提升**。

【次指标（风格归一口径）】对照组"过度设计命中"：S 7/8、8/8 → **D 5/8、5/8**，
即 D 的"简约审查"确实**减少了重型机制的提出**；但在裁判的成对判断中**未转化为优势**
（案例4 D2/S3、案例5 D2/S2）——**减少过度设计 ≠ 更贴近关键洞察**。

【关键：第一轮的"显著优势"被确认为仪器伪影】
  第一轮 token 口径：D 相对 S **+42pp**（并给出 p≈0.0000）
  第二轮风格归一 + 成对盲评：D 相对 S **−14pp**（36% vs 50%）
  一旦掐掉"长度/词密度"通道并改用成对判断，优势即消失 → **当初拒绝照 p 值下结论是对的**，
  且这次有了直接证据而非推断。

【本轮我自己修掉的一处混杂（由我自己的检查抓到）】D 协议第 3 步原文举例
"（数据库/分布式锁/消息队列）应被明确排除"——这些词**正是对照组判定"过度设计"的标记**，
等于把对照组答案提示给 D。已改为**不列举任何具体技术名词**并注明"列举会替你作答"。
另修：生成器未转义语料中的反引号（\`git status --porcelain\`）→ 生成的 spec 语法错误；
改为生成时转义（cc 收到的 prompt 内容不变）。

【两轮合计结论（已写入报告 §10.5）】
  · 验证类：两臂完全相同（各 3/5、误报 0/2）→ 该类别下协议无收益（基线已达天花板）；
  · 生成类：两轮均未观察到提升（第一轮不可判定 → 第二轮无显著差异、点估计偏 S）；
  · 可确定的：本会话每个真实缺陷都是靠**证据生产**（读源码/跑门禁/独立复算/对照实验/变异测试）抓到的，
    **没有一个来自多一轮讨论**；多方协议在本系统的实测收益未超噪声；
  · 代价侧：两轮共消耗约 100+ 分钟 cc 执行时间与相应配额，并间接暴露一次重派循环事故（§9.2）——
    这些成本应计入"是否值得再加一轮讨论"的决策。

【交付物】_EXPERIMENTS-2026-09-16.md 新增 §10（含仪器自检、成对结果、两轮对照、修订版综合结论）；
  证据：D:\\cc-tasks\\genexp2-result.json、genexp2-judge-case{1..5}.txt、genexp2-labels.json；
  校验：verify-genexp2-specs.mjs（15/15）、analyze-genexp2.mjs（仪器 + 主指标 + Wilson CI）。
`;

fs.appendFileSync(trace, body, 'utf8');
console.log('  追加字节 =', Buffer.byteLength(body, 'utf8'), '| 轨迹现字节 =', fs.statSync(trace).size);
