// 记录：S26 完成 + 两臂分布 + 决定性仪器校准（token 显著 vs 盲评反向）
import fs from 'node:fs';

const trace = String.raw`D:\dsh relay test\web-relay\traces\expr-2026-09-13_17-36-07.md`;
const now = new Date().toISOString();

const body = `
## [主 agent] ${now} — S 组 26 次跑完：token 口径显著（p≈0.0000），但被独立盲评校准**推翻**

【执行】26 份 S 复本分 3 个配额窗口跑完（12:22Z–14:44Z，中途 quota-paused 自动续跑）。
**独立无复用已验证**：26 个唯一 start 时间戳、全部 status=done、26/26 有报告。

【token 口径的两臂分布（样本量首次足够）】
  S（单方提议）: n=27  均值 **0.67/2**  案例1 19%(5/27)  案例2 48%(13/27)  分布 0分:11 1分:14 2分:2
  D（多角色反证）: n=26  均值 **1.15/2**  案例1 58%(15/26)  案例2 58%(15/26)  分布 0分:6 1分:10 2分:10
  观测均值差 0.487，置换检验（20000 次、xorshift32、双尾）**p ≈ 0.0000** → 拒绝"同分布"。
  **若就此收笔，结论会是"多角色反证显著更好"。**

【决定性校准：同一批输出过两台仪器（这是本轮最有价值的一步）】
从每臂**等距抽样 6 次运行**（非择优），specific 两例共 24 份答案匿名化后交外部 AI 盲评
（/ask + **显式传 workspacePath**，避免重演仓库污染），再与 token 口径在**同一 12 份/例**上对比：
  token 口径:  S 25%  vs  D 67%   → **D +42pp**
  独立盲评:    S 92%  vs  D 75%   → **S +17pp（方向相反）**
→ **token 的 D 优势未被独立盲评复现。**
  校准同时暴露盲评自身缺陷：**案例1 全部 12 份判 0、案例2 有 11/12 判 1**（地板/天花板效应，区分力≈0）。

【为什么 token 口径不可信（两处硬证据）】
1) **偏差与处理相关（混杂）**：D 的协议本身要求"每个结论必须引用证据、逐条核对"→ 产出天然更长、
   关键词更密；token 匹配器奖励词密度 → 可能只在测**答案风格**而非洞察。校准的反转与这一解释一致。
2) **案例1 的命中多半是偶然词命中**：token 称 D 案例1 命中 58%（15/26），而盲评对案例1 的 12 份**全判 0**；
   **两次独立裁判**（§3.5 对 run-1、本轮对 12 份样本）**一致判 0**。回看预登记词表，案例1 的"组2"含
   \`残留\`/\`已完成\`/\`跳过\` 等**通用词**，与"按链条身份隔离状态"这一洞察无关 → 命中很可能来自这些词。
   **词表设计缺陷（我的）**，但**不修改**（改了就是见结果后调判据）。

【最终结论】token：D 显著优于 S（p≈0.0000）；独立盲评（同批输出）：无显著差异且方向相反；
  两台仪器各有已证实缺陷 → **本实验仍为 inconclusive**。**不主张"多角色反证提升方案质量"**——
  那个 p 值来自一台与处理相关地偏置的仪器。
  唯一稳健发现：**案例1 的关键洞察几乎所有运行都没抓到**（两次独立裁判一致；
  token 的相反结论源于偶然词命中）——与 §3.6 的观察一致。
  要判定，须**先验证测量仪器**（多裁判平均 + 明令排除"长度/词密度"作为评分依据），
  或改用与风格无关的判定（如"是否给出可执行且正确的修复步骤"并由独立复算验证）。

【本轮又修掉两个我自己的缺陷（均已留痕）】
  · score-genexp-two-arms.mjs 的 RNG（\`seed * 1103515245\` 在 JS 下超 2^53 精度、退化）→ 改 xorshift32，
    并加**最小样本护栏**（任一臂 n<5 拒绝报告 p 值）+ 双向自检（同分布→p≈1、明显分离→p≈0）；
  · 校准脚本对 /ask **显式传 workspacePath**（今天已因漏传触发过一次仓库污染与 25 次重派循环）。

【交付物】_EXPERIMENTS-2026-09-16.md 的 §0/§9.1/§8.1/§5 已回填；
  证据：D:\\cc-tasks\\genexp-two-arms.json、judge2-prompt.txt、judge2-output.txt、judge2-mapping.json。
`;

fs.appendFileSync(trace, body, 'utf8');
console.log('  追加字节 =', Buffer.byteLength(body, 'utf8'), '| 轨迹现字节 =', fs.statSync(trace).size);
