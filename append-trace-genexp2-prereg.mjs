// 第二轮（out-of-sample）生成类实验预登记 —— 在**看到本轮任何结果之前**落盘
import fs from 'node:fs';

const trace = String.raw`D:\dsh relay test\web-relay\traces\expr-2026-09-13_17-36-07.md`;
const now = new Date().toISOString();

const body = `
## [主 agent] ${now} — 第二轮生成类实验预登记（新语料 + 改进仪器，out-of-sample）

【为什么重做而不"修第一轮"】第一轮结论为 inconclusive，两个原因都已定位：
  ① token 仪器的偏差**与处理相关**（D 的协议要求引用证据 → 答案更长更密；匹配器奖励词密度）；
  ② 单裁判有地板/天花板效应（案例1 全 0、案例2 11/12 判 1）。
  **不能拿第一轮那 53 份数据去调仪器**（我已看过结果 → 事后拟合只会得到"我想要的结论"）。
  故：换**新语料**（与第一轮不重叠）+ **改进后的仪器**，在本轮数据上检验。

【新语料（5 例，全部取自本会话后段的真实事件；反直觉且特定于本系统）】
  1. 失败重派策略 —— 真值：重试必须有次数上限/终止条件（实测持久性失败导致 25 次重派、约 87 分钟执行时间）
  2. 改动归属判定 —— 真值：git status 默认把未跟踪目录聚合为目录项 → 逐项 readFileSync 崩溃（EISDIR）→
     而崩溃被调用方读成"判定不合格"；须用 -uall + isFile 防御 + 可区分退出码
  3. 运行数据落盘位置 —— 真值：未显式传工作区时以 cwd 为基准 → 在仓库内生成 bin/web-relay/；
     而含中间斜杠的 .gitignore 模式**锚定根目录**故不匹配子目录 → 仓库变脏 → 被范围校验判越界 → 触发重派
  4. **对照组（过度设计）**：链条跑完后避免重复派发 —— 真值：一个幂等守卫即可；引入重型机制即为过度设计
  5. **对照组（过度设计）**：聚合持续增长的小目录 —— 真值：有界读取 + 容错即可；引入重型存储即过度设计
  说明：案例 4/5 是针对"对抗协议可能诱发为复杂而复杂"这一失败模式设的对照。

【仪器改进（在看到本轮任何结果之前固定）】
  ① **风格归一**：评前剥离 markdown 记号、折叠空白、**所有答案截断到同一长度** → 掐掉"长度/词密度"通道；
  ② **成对比较**取代绝对 0/1（成对判断远比绝对评分稳定，且直接给出胜率）；
  ③ **仪器自检（锚点）**：每例内附一对"金标准答案 vs 敷衍答案"，要求裁判必须选中金标准；
     **锚点不通过 → 该例的仪器判定作废**，而不是照用其结论；
  ④ 多例多对聚合出胜率与二项置信区间。

【预登记（此刻尚未跑任何一轮）】
  H1：多角色反证（含简约审查）在成对盲评中的胜率 > 50%；H0：= 50%。
  主指标：**成对盲评胜率**（风格归一后），按案例聚合并给 95% 二项 CI。
  次指标：风格归一后的 token 命中率（同一批输出）；两组对照的"净负面命中"（是否提出被 negTokens 命中的重型机制）。
  判定规则：**仅当仪器锚点全部通过后才计算胜率**；**仅当合并胜率的 95% CI 下界 > 50% 才判 D 更优**；
    若 CI 跨 50% → 判"本规模下无差异"。规则此刻固定，事后不得调整。
  规模：每臂 8 轮（16 任务，交错排列 S,D,S,D…，使两臂共享同一时间/配额压力以抵消漂移）。

【不变量（均已实测通过，verify-genexp2-specs.mjs 15/15）】
  16 份 spec 语法通过；同臂各轮 prompt 逐字节相同；两臂共通前缀仅到协议标题；
  prompt 保留反引号代码片段（转义不吃内容）；**ground truth 特征词未泄漏进任何一臂 prompt**；
  链条 16 项、spec 齐全、taskId 唯一、交错排列。
  ⚠ 本轮期间**我自己的泄漏检查抓到一处真混杂并已修**：D 协议第 3 步原文举例了
  "（数据库/分布式锁/消息队列）应被明确排除"——这些词**正是案例 4/5 判"过度设计"的标记**，
  等于把对照组答案提示给了 D。已改为**不列举任何具体技术名词**并注明"列举会替你作答"。

【装填】用新的指针式装载器 \`arm-chain.mjs\`（不再手改 run-chain.cmd）；v7-gen2 各项无 planStepId，
故不带 --close-after-accept（装载器会对该组合直接拒绝，本次不会触发）。
`;

fs.appendFileSync(trace, body, 'utf8');
console.log('  追加字节 =', Buffer.byteLength(body, 'utf8'), '| 轨迹现字节 =', fs.statSync(trace).size);
