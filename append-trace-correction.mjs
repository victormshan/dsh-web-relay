// 记录：第 26 次干净复核（通过验收）的评分结果 —— 推翻"打平"表述这一更正
import fs from 'node:fs';

const trace = String.raw`D:\dsh relay test\web-relay\traces\expr-2026-09-13_17-36-07.md`;
const now = new Date().toISOString();

const body = `
## [主 agent] ${now} — 更正：干净复核（第 26 次）推翻"打平"表述，实验二改判为不可判定

【发现了什么】在写 §9 复核小结后做例行状态核对时，发现 chain-state 已变为
\`chainId=v6-gen-d index=1 项=accepted-awaiting-review\` —— 即 **20:00 那一次（第 26 次）已经通过验收**
（因为我在它执行 verify 之前已清掉仓库污染 \`bin/web-relay/\`）。这是**用户要求的"干净复核"**，
它此前被我漏掉（我把注意力放在 25 份归档上）。

【该次评分（与 S 组同一套预登记 token 口径）】
  D 第 26 次：specific **2/2（满分）**、全例 **4/4**、结构完整、done.flag=true、verify-cc-task **ACCEPT 7/7**
  S（n=1）：specific 1/2、全例 3/4
  D 26 次总览：run-1 = 0/2（被配额终止）；25 次归档均值 1.12/2（案例1 14/25、案例2 14/25、得 0/2 有 6/25）；第 26 次 = 2/2

【必须做的更正】我在 §3.5 与 §9 初稿里写过"校准后两臂打平"。**该表述基于两臂各 n=1 的比较，现被推翻**：
  - 干净复核（n=1）给出的是 **D 2/2 > S 1/2**（D 更好），而非打平；
  - 但 26 次 D 的得分**横跨 0–2**（均值≈1.15）→ **组内方差压倒两臂差异**；
  - 用任意一次 D（0/2 或 2/2）去对 S 的单次观测，都能得出相反结论 —— 这正是 n=1 比较的伪影。
  → 实验二的诚实结论是 **inconclusive（以当前设计无法判定）**，既不是"打平"，也不是"D 更好"。
  → 三个并列原因：① S 只有 n=1；② token 仪器有系统性假阴性且偏差随措辞而变；③ 生成任务随机性极大。

【唯一缺失的一步（已写入 §9.1）】把 S 也跑同样的轮数（每次约 3 分钟、只读、不占额外机制成本），
然后比较**两臂分布**而非单次值。

【文档更正清单（均已落盘）】
  · 状态行、§0 结论速览、§3.5 结论、§7 待补、§8.1 第 2 行、§8.2 第 1 条 → 统一收敛为 inconclusive；
  · §9.1 整节重写为"推翻了'打平'这一表述"，含 26 次总览表与更正说明；
  · §5 自我更正清单新增第 7 条：**用两臂各 n=1 的比较宣布"打平"** —— 教训：对随机系统做单次对照，
    结论必然是伪影；并把第 8–10 条（/ask 漏传 workspacePath、重试无终止条件、验收器崩溃=拒绝）一并列入。

【方法学教训（本次最值钱的一条）】
  本次之所以能自我推翻，是因为**保留了 26 次原始产物**并做了全量评分——若只保留 run-1，
  "打平"会被当成结论写死。**保留原始重复数据、并在结论出炉后仍做例行状态核对**，
  是这次更正得以发生的两个直接原因。
`;

fs.appendFileSync(trace, body, 'utf8');
console.log('  追加字节 =', Buffer.byteLength(body, 'utf8'), '| 轨迹现字节 =', fs.statSync(trace).size);
